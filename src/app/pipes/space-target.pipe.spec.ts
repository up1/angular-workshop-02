import { SpaceTargetPipe } from './space-target.pipe';

describe('SpaceTargetPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceTargetPipe();
    expect(pipe).toBeTruthy();
  });
});
